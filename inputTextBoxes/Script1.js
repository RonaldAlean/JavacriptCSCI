// JavaScript source code
function MyFunction() {
  //  alert("ASFIK");

    var firstName = document.getElementById("fname").value; 
   alert(firstName);
    var lastName = document.getElementByID("lname").value;
    //alert(lastName);
    var comm = document.getElementByID("textAR").value;
    //alert(comm);

    //alert(firstName + "  " + lastName + "  " + comm);

    /*document.writeln("<h1>" + "</h1>");*/

    /*
    var z = document.getElementById("abc");

    var s;
    for (s = 0; s < z.length; s++) {
        if (z[s].checked)
            alert(z[s].value);
    }

    var optionlist = documentnt.getElementById('selectClass').options;

    for (var option = 0; option < optionlist.length; option++) {
        if (optionlist[option].selected == true) {
            alert(optionlist[option].value)
            break;
        }
    }*/
}